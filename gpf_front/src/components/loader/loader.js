import React from "react";
import "./loader.css"; // Estilos del componente

const Loading = () => {
  return (
    <div className="loading-container">
      <div className="loader"></div>
    </div>
  );
};

export default Loading;